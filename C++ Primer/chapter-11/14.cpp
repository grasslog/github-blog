#include <bits/stdc++.h>
using namespace std;

int main(int argc, char **argv)
{
    map<string, vector<string>> family;
    vector<pair<string,string>> child;
    string first_name, child_name, _birthday;
    cout << "Input end with end." << endl;

    while([&]()->bool { cout<<""})
}