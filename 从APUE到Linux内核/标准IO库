  当Linux新建一个进程时，会自动创建3个文件描述符0、1和2，分别对应标准输入、标准输出和错误输出。C库中与文件描述符对应的是文件指针，与文件描述符0、1和2类似，我们可以直接使用文件指针stdin、stdout和stderr。那么这就是否意味着stdin、stdout和stderr是“自动打开”的文件指针呢？
  从源码可以看出，stdin、stdout和stderr是宏定义，所以在C库的代码又定义了同名宏。
  可以得到，stdin、stdout和stderr都是FILE类型的文件指针，是由C库静态定义的，直接与文件描述符0、1和2相关联，所以应用程序可以直接使用他们。
